# Optional. Same smoke as `docker build` / `scripts/docker-smoke.sh`.
# Do not add platform: linux/amd64 — cts-ai is linux/arm64.
services:
  smoke:
    build: .
    command: ["./scripts/qemu-smoke.sh"]
