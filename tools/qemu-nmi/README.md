# tools/qemu-nmi — pinned QEMU for ADR-083

Build output of `./scripts/build-qemu-nmi.sh` lands here (`bin/`, `share/`).
Those directories are **gitignored** — rebuild locally / in opt-in CI.

See `PIN.txt` and `research/qemu-nmi/README.md`.
