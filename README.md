# ctos

A minimal Rust OS project skeleton with a simple kernel configuration and FIFO scheduler for toy processes.

## Features

- Kernel memory layout modeling
- Process and scheduler abstractions
- Basic verification tests via `cargo test`

## Run

```bash
cargo run
```
