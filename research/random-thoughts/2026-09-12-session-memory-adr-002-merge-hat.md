# Session memory — 2026-09-12 (ADR-002 merge-hat cleanup)

Base `main` `ba6541c`. Docs-only.

`gh pr list` on this repo: every merged PR including Track A showed `author=artofdream` and `mergedBy=artofdream`. Daily briefs that “expected `cursor[bot]`” as GitHub author were wrong. Commits on A1–A9 were typically `cursoragent` with `Co-authored-by: artofdream`. `cursor[bot]` exists (id 206951365) and left a COMMENT on #60; it did not merge that PR.

Named exception scoped to Track A A1–A9 as sponsor asked. Noted earlier miles share the same GitHub pair without relitigating them.

Do not treat this file as the ledger.
