# Daily brief — 2026-09-09 (M7 paging + frame allocator)

## Where we stopped

PR #12 merged at `9ebc4d0` (`cursor/m7-paging-frame-allocator-b567`). Parent was `main` `589cfef`. M7 / FR-09: EL1 identity map, bump frames from `__kernel_end` through virt 128 MiB RAM, serial `paging: ok`, ADR-008.

DTB walk is staged. Heap / `GlobalAlloc` is M8. Scheduler is M9.

Cloud `scripts/qemu-smoke.sh` **Verified** (2026-09-09, QEMU 8.2.2): `Hello World!`, `paging: ok`, `timer: tick`, `input: rx 0x41`, two `exception: sync BRK`, `exception: fatal nested` / `kind=0x200`, `Running 14 tests` all `[ok]`, force-fail exit 1.

GHA `smoke.yml`: **Verified** — PR [34393902440](https://github.com/artofdream/ctos/actions/runs/34393902440) and push [34393896252](https://github.com/artofdream/ctos/actions/runs/34393896252) success (`ubuntu-24.04` + `ubuntu-24.04-arm`) on `2b509d0`.

Owner (`artofdream`) merged #12. ADR-002 prefers `cursor[bot]` when the owner opened the PR.

## Do next

1. Human or MRC review of this docs PR. Author does not merge (ADR-002).
2. After this ledger lands: M8 is heap. Do not start it here.

## Honesty

- cts-ai offline. Docker Desktop path not re-run.
- DTB parse / higher-half / guard pages / `GlobalAlloc`: Unknown (not this path).
