# Daily brief — 2026-09-09 (M3 VBAR_EL1)

## Where we stopped

Branch `cursor/m3-vbar-el1-exceptions-bd73`. One milestone: CPU exceptions (FR-06). `VBAR_EL1` table, current-EL sync `BRK` (resumable), park stubs, `#[test_case]`, qemu-smoke greps the handler string. ADR-004 records EL1 + context format.

## Do next

1. Probe on this cloud VM (`scripts/qemu-smoke.sh`) and GHA `smoke.yml`. Ledger rows stay Unknown until those probes.
2. Human or MRC review. Author does not merge (ADR-002). If this PR is `cursor[bot]`, owner `artofdream` merges.
3. After merge: M4 (fatal exception stack / FR-07), not GIC.

## Honesty

- cts-ai is offline this session. Docker Desktop path not re-run.
- Taken lower-EL / IRQ exceptions: Unknown (stubs only).
