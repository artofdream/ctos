# Daily brief — 2026-09-09 (M8 heap / GlobalAlloc)

## Where we stopped

PR for `cursor/m8-heap-globalalloc-0ee8`. Parent is `main` `9ebc4d0` (Merge PR #12, M7 / FR-09). M8 / FR-10 only: first-fit `GlobalAlloc` on 16 identity-mapped frames, `extern crate alloc`, serial `heap: ok`, ADR-009.

Scheduler is M9. Do not start it on this PR.

Cloud `scripts/qemu-smoke.sh` **Verified** (2026-09-09, QEMU 8.2.2): `Hello World!`, `paging: ok`, `heap: ok`, `timer: tick`, `input: rx 0x41`, two `exception: sync BRK`, `exception: fatal nested` / `kind=0x200`, `Running 17 tests` all `[ok]`, force-fail exit 1.

GHA `smoke.yml` **Verified**: [34394697056](https://github.com/artofdream/ctos/actions/runs/34394697056) (`ubuntu-24.04-arm` + `ubuntu-24.04`) on the cloud-probe commit `ba6096a`.

## Do next

1. Human or MRC review. Author does not merge (ADR-002). Merge hat: `artofdream` if GitHub shows `cursor[bot]`; `cursor[bot]` if the owner opened it.
2. After merge: M9 is the cooperative scheduler. Do not start it on this PR.

## Honesty

- cts-ai offline. Docker Desktop path not re-run.
- Growing heap / slab / userspace maps / M9: Unknown (not this path).
