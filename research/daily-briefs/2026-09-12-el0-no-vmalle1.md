# ADR-039 — EL0 entry without TLBI VMALLE1

Date: 2026-09-12. (evo-x2) intent; implementation on agent box clone of `artofdream/ctos` at `origin/main` `f2c0c5a` (CloudAgent HELD; preferred Windows machineId not used).

After ADR-037/038 identity `.data`/heap tear, drop `TLBI VMALLE1` from standing/EL0 trampoline (`eret_to_el0`, `stay_at_el0`, `sync_lower_el`). Keep MMU-enable `VMALLE1` and per-VA `VAAE1`. Serial `el0: no-vmalle1`. Do not claim EL0 isolated. PAN enable stays Planned.
