# Daily brief — 2026-09-09 (M5 GIC + timer)

## Where we stopped

PR branch `cursor/m5-hardware-interrupts-d5a6`. Parent is `main` `180dbf2` (Merge PR #7, M4 / FR-07). M5 / FR-08 only: GICv2 (default `-machine virt`), CNTP + PPI 30, current-EL IRQ vector, serial `timer: tick`, ADR-006.

M2–M4 smoke strings stay required. UART input is **M6** — not this PR.

## Do next

1. Run `scripts/qemu-smoke.sh` on this cloud VM; record Verified or Failed.
2. Wait for GHA; record Verified or leave Unknown.
3. Human or MRC review. Author does not merge (ADR-002). Merge hat: `artofdream` if GitHub shows `cursor[bot]`; `cursor[bot]` if the owner opened it.

## Honesty

- cts-ai offline. Docker Desktop path not re-run.
- GICv3 / `virtualization=on` / taken FIQ: Unknown.
- Parent includes M4 (`7b45db3` / `ce1b4e5` are ancestors of `180dbf2`).
