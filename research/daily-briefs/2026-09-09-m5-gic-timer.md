# Daily brief — 2026-09-09 (M5 GIC + timer)

## Where we stopped

PR https://github.com/artofdream/ctos/pull/8 (`cursor/m5-hardware-interrupts-d5a6`). Parent is `main` `180dbf2` (Merge PR #7, M4 / FR-07). M5 / FR-08 only: GICv2 (default `-machine virt`), CNTP + PPI 30, current-EL IRQ vector, serial `timer: tick`, ADR-006.

Cloud `scripts/qemu-smoke.sh` **Verified** (2026-09-09, QEMU 8.2.2): `Hello World!`, `timer: tick`, two `exception: sync BRK`, `exception: fatal nested` / `kind=0x200`, `Running 9 tests` all `[ok]`, force-fail exit 1.

GHA `smoke.yml` **Verified**: [34391557761](https://github.com/artofdream/ctos/actions/runs/34391557761) (`ubuntu-24.04` + `ubuntu-24.04-arm`) on the cloud-probe commit; [34391552019](https://github.com/artofdream/ctos/actions/runs/34391552019) on the implementation commit.

UART input is **M6** — not this PR.

## Do next

1. Human or MRC review. Author does not merge (ADR-002). Merge hat: `artofdream` if GitHub shows `cursor[bot]`; `cursor[bot]` if the owner opened it.
2. After merge: M6 is UART/virtio input. Do not start it on this PR.

## Honesty

- cts-ai offline. Docker Desktop path not re-run.
- GICv3 / `virtualization=on` / taken FIQ: Unknown.
- Parent includes M4 (`7b45db3` / `ce1b4e5` are ancestors of `180dbf2`).
