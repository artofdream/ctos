# 2026-09-13 — FAT16 write (Future slice 3 / ADR-050)

## Done
- H1: guest FAT16 write behind thin VFS (`vfs::write` on `/probe` + restore) and FAT backend create of `/fwr`.
- Serial: `fat: write` / `fat: rewrite` / `fat: create` / keep `fat: ok`.
- Host: `scripts/mkfat16.py --check-write` sees `/fwr=fat-nw` and restored `/probe`.
- ADR-050, docs, threat-model v1.28, smoke ratchet. `vfs::create` stays memfs-first (A6).
- agent-box `qemu-smoke: ok`, 96 tests. Do not self-merge.

## Not done
- Multi-cluster grow, `vfs::create` → FAT without breaking A6, `readdir`, delete.
- (evo-x2) re-probe (CloudAgent HELD).
