# Daily brief — 2026-09-12 (Track A / A2 libctos CRT)

## Where we stopped

Draft PR https://github.com/artofdream/ctos/pull/52 (`cursor/libctos-crt-a2-590e`). Parent is `main` `f81df18` (A1 / ADR-021). One PR: ADR-022 freestanding CRT / `libctos`. No new FR/NFR IDs. App hosting unclaimed. A3–A9 stay **Planned**.

Cloud `qemu-smoke` **Verified** on this tip (2026-09-12, QEMU 8.2.2, `rustc` 1.100.0-nightly `0fc141305`): host `perf: elf-size bytes=4024936`; `libctos: hi` / `libctos: ok` / `libctos: linked` plus prior `svc:` / `el0:` / ident/ttbr1/asid/paging/heap/sched/wx/guard/ro/perf markers; `Running 57 tests` all `[ok]`; force-fail exit 1.

First hello trip Failed: Rust `main` `str x30, [sp, #-0x10]!` on the EL0-exec page (AP = EL0 no data; `SCTLR.WXN` forbids W+X). Fixed with a second EL0-RW NX stack page.

## Do next

1. Human or MRC review. Author does not merge (ADR-002). GitHub author of #52 is `cursor[bot]`; merge hat is `artofdream`.
2. A3: ELF (or raw) loader into user TTBR0 ([#34](https://github.com/artofdream/ctos/issues/34)). Do not claim app hosting. Isolation / PAN still Planned.

## Honesty

- Cloud Verified is this QEMU virt guest on the stack-page tip. GHA on this tip stays Unknown until grepped.
- Did not claim userspace, POSIX, a guest ELF loader, or that Track A is done.
