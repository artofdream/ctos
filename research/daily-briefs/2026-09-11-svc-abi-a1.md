# Daily brief — 2026-09-11 (Track A / A1 SVC ABI)

## Where we stopped

Draft PR https://github.com/artofdream/ctos/pull/49 (`cursor/svc-syscall-abi-0dc5`). Parent is `main` `e80dc93` (Merge PR #28 / ADR-020). One PR: ADR-021 minimal EL0 SVC ABI. No new FR/NFR IDs. App hosting unclaimed.

Cloud `scripts/qemu-smoke.sh` **Verified** on `79afb57` (2026-09-11, QEMU 8.2.2, `rustc` 1.100.0-nightly `67eda617e`): host `perf: elf-size bytes=4020496`; `svc: yield` / `svc: user-hi` / `svc: uart n=13` / `svc: exit` / `svc: ok` plus prior ident/ttbr1/standing/asid/paging/heap/sched/wx/guard/ro/el0/perf markers; `Running 53 tests` all `[ok]`; force-fail exit 1.

Track A A2–A9 stay **Planned** (no ELF loader, no `libctos`, no VFS).

## Do next

1. Human or MRC review. Author does not merge (ADR-002). GitHub author of #49 is expected `cursor[bot]`; merge hat is `artofdream`.
2. Do not claim app hosting or a Linux ABI. Next Track A work is A2 (`libctos`) on a new branch.
3. Isolation / PAN still Planned.

## Honesty

- Cloud Verified is this QEMU virt guest on `79afb57`.
- Did not claim userspace, POSIX, or that Track A is done.
