# Daily brief — 2026-09-11 (Track A / A1 SVC ABI)

## Where we stopped

Draft/ready PR https://github.com/artofdream/ctos/pull/49 (`cursor/svc-syscall-abi-0dc5`). Rebased onto `main` `6c34e4a` (PR #51 plain-English + mermaid). One PR: ADR-021 minimal EL0 SVC ABI. No new FR/NFR IDs. App hosting unclaimed.

#51 Pages/HTTPS rows kept as #51 left them (live site **Verified**, custom-domain HTTPS **Verified**). Not re-probed on this rebase. A1 ABI rows are **Unknown** until `qemu-smoke` on the new tip.

Track A stays **incomplete** after A1. A2–A9 stay **Planned** (no ELF loader, no `libctos`, no VFS).

## Do next

1. Re-run `./scripts/qemu-smoke.sh` on this tip. Author does not merge (ADR-002). GitHub author of #49 is `artofdream`; merge hat is `cursor[bot]`. This implementing session does not merge.
2. Do not claim app hosting or a Linux ABI. Next Track A work is A2 (`libctos`) on a new branch.
3. Isolation / PAN still Planned.

## Honesty

- Prior cloud/GHA Verified captures (`773f64b`, `0638174`) are not this tip.
- Did not claim userspace, POSIX, or that Track A is done.
