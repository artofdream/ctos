# Daily brief — 2026-09-09 (M6 PL011 UART RX)

## Where we stopped

PR https://github.com/artofdream/ctos/pull/10 (`cursor/m6-uart-rx-input-7ef6`). Parent is `main` `17950f6` (Merge PR #8, M5 / FR-08 timer). M6 / FR-08 input only: PL011 `try_recv`, host-injected `0x41` after `Hello World!`, serial `input: rx 0x41`, ADR-007.

QEMU 8.2 does not implement UARTCR.LBE. virtio-input was skipped (virtqueues / DMA, no paging yet).

Cloud `scripts/qemu-smoke.sh` **Verified** (2026-09-09, QEMU 8.2.2): `Hello World!`, `timer: tick`, `input: rx 0x41`, two `exception: sync BRK`, `exception: fatal nested` / `kind=0x200`, `Running 10 tests` all `[ok]`, force-fail exit 1.

GHA `smoke.yml`: **Unknown** until a run URL on the cloud-probe commit.

## Do next

1. Wait for GHA; record Verified or leave Unknown.
2. Human or MRC review. Author does not merge (ADR-002). Merge hat: `artofdream` if GitHub shows `cursor[bot]`; `cursor[bot]` if the owner opened it.
3. After merge: M7 is paging. Do not start it on this PR.

## Honesty

- cts-ai offline. Docker Desktop path not re-run.
- UART RX IRQ / virtio-keyboard / QEMU LBE: Unknown (not this path).
