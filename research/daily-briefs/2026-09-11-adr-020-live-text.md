# Daily brief — 2026-09-11 (ADR-020 live identity `.text`)

## Where we stopped

Draft PR for `cursor/adr-020-live-text-reloc-322c`. Parent is `main` `24d94e6` (Merge PR #27 / ADR-019). One PR: ADR-020 high-VA vtable / fn-pointer rewrite + live identity `.text` tear after the boot stub. No new FR/NFR IDs. PAN unclaimed.

ELF probe on `24d94e6` confirmed the #27 hypothesis: `Pl011` `dyn Write` vtable in `.rodata` holds identity method pointers; hello `.rodata` had 9–12 such words; `.data`/`.text` had no 8-byte identity literal pools; codegen is `ADRP` (no `MOVZ`/`MOVK` of `0x4008`). `.text` and `.rodata` shared a page on the ADR-019 layout — linker now page-splits them.

Cloud `scripts/qemu-smoke.sh` **Verified** on `0fda690` (2026-09-11, QEMU 8.2.2, `rustc` 1.100.0-nightly `67eda617e`): host `perf: elf-size bytes=3988408`; `ident: jump` / `ident: reloc n=12` / `ident: range lo=0x400ae000 hi=0x400b2000 pages=4` / `ident: live lo=0x40081000 hi=0x400a6000 pages=37` plus prior ident/ttbr1/standing/asid/paging/heap/sched/wx/guard/ro/el0/perf markers; `Hello World!` after the live tear; hello BRK ELRs high; `Running 50 tests` all `[ok]`; force-fail exit 1.

`.rodata` / `.data` / heap stay identity-mapped. `_start` stays at `0x4008_0000`. Full identity teardown stays **Planned**.

## Do next

1. Human or MRC review. Author does not merge (ADR-002).
2. Do not claim “EL0 isolated” or “the kernel moved.” Next identity work is `.rodata` / `.data` / heap, only after those accesses are proven high-only.
3. PAN still needs `ID_AA64MMFR1_EL1.PAN != 0`. Do not switch `-cpu`.

## Honesty

- Cloud Verified is this QEMU virt guest on `0fda690`.
- Did not claim PAN or that identity mappings were fully torn down.
