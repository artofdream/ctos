[package]
name = "hello-libctos"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
libctos = { path = "../../libctos" }

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"
